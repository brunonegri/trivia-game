/* import { SET_SCORE } from '../actions/ActionsType';

const initialState = {
  score: 0,
  timer: 0,
  difficulty: 0,
};

const scoreReducer = (state = initialState, action) => {
  switch (action.type) {
  case SET_SCORE:
    return { ...state,

      timer: action.payload.timer,
      difficulty: action.payload,
    };
  default:
    return state;
  }
};

export default scoreReducer; */
